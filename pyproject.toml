[project]
name = "scraper-test"
version = "0.1.0"
description = "Scraper inteligente con OpenRouter"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai",
    "python-dotenv",
]

[project.scripts]
run-scraper = "main:main"
